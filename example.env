APP_ENV=production
DATABASE_URL=
HEALTH_CHECK_URL=
FACTORY_CONTRACT_ID=
REWARDS_CONTRACT_ID=
FIAT_SAFE_ADDRESS=
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
# getEvents endpoint used to replay ledgers the live tail has scrolled past. MAY
# be the same URL as SOROBAN_RPC_URL. Unset = no replay: gaps below live
# retention are skipped instead.
BACKFILL_SOURCE_URL=https://soroban-rpc.testnet.stellar.gateway.fm
START_LEDGER=0
POLL_INTERVAL_MS=5000
